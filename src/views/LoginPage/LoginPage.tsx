import { Container, FormWrapper } from '@/shared/ui'
import { LoginForm } from './ui/LoginForm'

export const LoginPage = () => {
  return (
    <Container>
      <FormWrapper>
        <LoginForm />
      </FormWrapper>
    </Container>
  )
}
